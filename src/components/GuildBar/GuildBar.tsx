import React from 'react'
import s from './GuildBar.module.scss';

type Props = {}

const GuildBar = (props: Props) => {
  return (
    <div className={s.content}>GuildBar</div>
  )
}
export default GuildBar;